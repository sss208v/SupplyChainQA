# 1. Lint-staged: format staged files
npx lint-staged || exit 1

# 2. Backend unit tests (skip integration tests)
# 不带覆盖率门禁：覆盖率门禁单一归属于 .github/workflows/ci.yml 的测试步骤（见 AGENTS.md “编辑后验证流程”）
# 用 venv 内的 Python 3.11（含全部依赖），不要用全局 python（版本/依赖可能不符）；子 shell 隔离目录切换
(cd backend && venv/Scripts/python.exe -m pytest tests -q -k "not integration") || exit 1

# 3. Frontend unit tests
(cd frontend && npx vitest run) || exit 1
